{
  "scripts": {
    "start": "npm run clean && npm run build && open index.html",
    "build": "webpack --mode=development --no-devtool",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "html-react-parser": "../../",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "webpack": "^5.18.0",
    "webpack-cli": "^4.4.0"
  }
}
