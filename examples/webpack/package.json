{
  "scripts": {
    "start": "npm run clean && npm run build && open index.html",
    "build": "webpack --mode=development --no-devtool",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "html-react-parser": "../../",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "webpack": "^5.75.0",
    "webpack-cli": "^5.0.1"
  }
}
